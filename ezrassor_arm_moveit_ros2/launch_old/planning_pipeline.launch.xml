<launch>

  <!-- This file makes it easy to include different planning pipelines;
       It is assumed that all planning pipelines are named XXX_planning_pipeline.launch  -->

  <let name="pipeline" default="ompl" />

  <!-- define capabilites that are loaded on start (space seperated) -->
  <let name="capabilities" default=""/>

  <!-- inhibit capabilites (space seperated) -->
  <let name="disable_capabilities" default=""/>

  <group>
    <include file="$(find-pkg-share ezrassor_arm_moveit_ros2)/launch/$(var pipeline)_planning_pipeline.launch.xml">
      <let name="capabilities" value="$(var capabilities)"/>
      <let name="disable_capabilities" value="$(var disable_capabilities)"/>
    </include>
  </group>

</launch>
