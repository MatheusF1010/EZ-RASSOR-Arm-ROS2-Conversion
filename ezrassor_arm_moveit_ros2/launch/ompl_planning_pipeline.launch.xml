<launch>

  <!-- OMPL Plugin for MoveIt! -->
  <let name="planning_plugin" value="ompl_interface/OMPLPlanner" />

  <!-- define capabilites that are loaded on start (space seperated) -->
  <arg name="capabilities" default=""/>

  <!-- inhibit capabilites (space seperated) -->
  <arg name="disable_capabilities" default=""/>

  <!-- The request adapters (plugins) used when planning with OMPL.
       ORDER MATTERS -->
  <let name="planning_adapters" value="default_planner_request_adapters/AddTimeParameterization
				       default_planner_request_adapters/FixWorkspaceBounds
				       default_planner_request_adapters/FixStartStateBounds
				       default_planner_request_adapters/FixStartStateCollision
				       default_planner_request_adapters/FixStartStatePathConstraints" />

  <let name="start_state_max_bounds_error" value="0.1" />

<!-- Stops working here -->

  <!-- <param name="planning_plugin" value="$(var planning_plugin)" />
  <param name="request_adapters" value="$(var planning_adapters)" />
  <param name="start_state_max_bounds_error" value="$(var start_state_max_bounds_error)" />
  <param name="capabilities" value="$(var capabilities)" />
  <param name="disable_capabilities" value="$(var disable_capabilities)" /> -->

  <!-- <rosparam command="load" file="$(find-pkg-share ezrassor_arm_moveit_ros2)/config/ompl_planning.yaml"/> -->

</launch>
